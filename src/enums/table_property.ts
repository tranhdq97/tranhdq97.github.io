export enum ETPCommon {
  NAME = "name",
  ID = "id",
  TYPE = "type",
  TYPE_ID = "type_id",
  IS_AVAILABLE = "is_available",
  CREATED_AT = "created_at",
  UPDATED_AT = "updated_at",
  PROFILE = "profile",
  PROFILE_ID = "profile_id",
  PHOTO = "photo",
  PHOTO_ID = "photo_id",
  DESC = "desc",
  IS_DELETED = "is_deleted",
  DETAIL = "detail",
}

export enum ETPStaff {
  EMAIL = "email",
  PASSWORD = "password",
}

export enum ETPProfile {
  FIRST_NAME = "first_name",
  LAST_NAME = "last_name",
  ADDRESS = "address",
  PHONE_NUMBER = "phone_number",
  DOB = "dob",
  SEX = "sex",
  SEX_ID = "sex_id",
}

export enum ETPAddress {
  DISTRICT = "district",
  DISTRICT_ID = "district_id",
}

export enum ETPMasterDistrict {
  ZIPCODE = "zipcode",
}

export enum ETPMasterCity {
  COUNTRY = "country",
  COUNTRY_ID = "country_id",
}
